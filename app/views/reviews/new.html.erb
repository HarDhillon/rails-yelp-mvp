<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :rating %>
  <%= f.input :content %>
  <%= f.submit %>
<% end %>

<%= link_to "Back to Restaurant", "/restaurants/#{@restaurant.id}", class: "btn btn-primary" %>